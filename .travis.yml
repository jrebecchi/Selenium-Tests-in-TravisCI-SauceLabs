sudo: false

language: node_js
node_js:
  - 10

dist: trusty

jobs:
   include:
    - stage: "Tests"
      script: npm run test

before_install:
  - npm install

addons:
  sauce_connect:
    username: "JohannC"
    access_key: SAUCE_ACCESS_KEY